# M1: 稳定基石 (v0.4.3) 工作执行计划

**项目版本**: v0.4.3
**里程碑**: M1 - 稳定基石
**计划日期**: 2026-03-25
**目标日期**: 2026-04-01
**核心目标**: 消除生产风险，完善基础架构

***

## 一、里程碑概述

### 1.1 目标描述

v0.4.3 是稳定加固版本，核心目标是消除当前存在的生产风险，完善基础架构，为后续商业功能开发奠定稳定基础。

### 1.2 关键成果

| 类别       | 成果                              |
| -------- | ------------------------------- |
| **权限系统** | usePermissions hook 恢复，前后端完整集成  |
| **稳定性**  | ErrorBoundary 防止白屏，Redis 生产风险修复 |
| **性能**   | 数据库索引添加，Prisma 连接池优化            |
| **可观测性** | 健康检查端点，API 统一错误处理               |

### 1.3 资源投入

| 角色     | 工作量     |
| ------ | ------- |
| 前端     | 6h      |
| 后端     | 16h     |
| DevOps | 2h      |
| **总计** | **24h** |

***

## 二、详细任务分解

### 2.1 P0 紧急任务 (必须完成)

#### 任务 1: 恢复 usePermissions hook

**工作量**: 2h
**负责人**: 前端
**依赖**: 无
**验收标准**: 权限前端完整可用

**具体步骤**:

1. 创建 `src/hooks/usePermissions.tsx`
2. 实现 PermissionProvider 和 usePermissions hook
3. 在 AdminLayout 中集成 PermissionProvider
4. 创建 RequirePermission 组件
5. 测试权限检查功能

**代码位置**: `src/hooks/usePermissions.tsx`

**实现要点**:

```Python
// 必须包含功能
- permissions 状态管理
- hasPermission(permission)
- hasAnyPermission(permissions)
- hasAllPermissions(permissions)
- isLoading 状态
- refresh() 方法
```

***

#### 任务 2: 添加 ErrorBoundary

**工作量**: 1h
**负责人**: 前端
**依赖**: 无
**验收标准**: 子组件崩溃不白屏

**具体步骤**:

1. 创建 `src/components/error/ErrorBoundary.tsx`
2. 实现 getDerivedStateFromError
3. 实现 componentDidCatch
4. 在根布局中集成
5. 测试错误捕获

**代码位置**: `src/components/error/ErrorBoundary.tsx`

***

#### 任务 3: 修复 Redis Mock 生产风险

**工作量**: 1h
**负责人**: 后端
**依赖**: 无
**验收标准**: 生产环境强制验证

**具体步骤**:

1. 修改 `src/lib/redis.ts`
2. 生产环境强制验证环境变量
3. 开发环境允许 Mock
4. 添加明确的错误提示
5. 测试生产环境报错

**代码位置**: `src/lib/redis.ts`

**实现要点**:

```typescript
// 生产环境必须抛出错误
if (process.env.NODE_ENV === "production") {
  if (!url || !token) {
    throw new Error("生产环境必须配置 UPSTASH_REDIS_REST_URL 和 UPSTASH_REDIS_REST_TOKEN")
  }
}
```

***

#### 任务 4: 添加数据库索引

**工作量**: 2h
**负责人**: 后端
**依赖**: Prisma
**验收标准**: 订单/商品查询加速

**具体步骤**:

1. 修改 `prisma/schema.prisma`
2. Order 模型添加索引
3. Product 模型添加索引
4. Payment 模型添加索引
5. 执行 `prisma migrate dev`
6. 验证查询计划

**代码位置**: `prisma/schema.prisma`

**索引列表**:

```prisma
// Order 模型
@@index([userId])
@@index([status])
@@index([createdAt])
@@index([userId, status])

// Product 模型
@@index([categoryId])
@@index([isPublished])
@@index([name])
@@index([sku])

// Payment 模型
@@index([orderId])
@@index([transactionId])
```

***

#### 任务 5: 添加健康检查端点

**工作量**: 1h
**负责人**: 后端
**依赖**: 无
**验收标准**: /api/health 可访问

**具体步骤**:

1. 创建 `src/app/api/health/route.ts`
2. 实现数据库连接检查
3. 实现 Redis 连接检查
4. 返回健康状态
5. 配置 Netlify/Vercel 健康检查

**代码位置**: `src/app/api/health/route.ts`

**实现要点**:

```typescript
// 返回格式
{
  status: "healthy" | "unhealthy",
  checks: {
    database: { status: "ok" | "error" },
    redis: { status: "ok" | "error" },
    timestamp: "2026-03-25T12:00:00Z"
  }
}
```

***

### 2.2 P1 重要任务

#### 任务 6: 统一 API 错误处理

**工作量**: 4h
**负责人**: 后端
**依赖**: 无
**验收标准**: 统一 AppError 类

**具体步骤**:

1. 创建 `src/lib/errors.ts`
2. 定义 AppError 类
3. 创建 API 包装器 `src/lib/api-wrapper.ts`
4. 重构现有 API 路由
5. 测试错误处理

**代码位置**: `src/lib/errors.ts`, `src/lib/api-wrapper.ts`

**实现要点**:

```typescript
// AppError 类
class AppError extends Error {
  constructor(
    public code: string,
    public message: string,
    public statusCode: number = 500
  ) {
    super(message)
  }
}

// API 包装器
export function withHandler(handler) {
  return async (req, ctx) => {
    try {
      return await handler(req, ctx)
    } catch (error) {
      if (error instanceof AppError) {
        return NextResponse.json(
          { success: false, error: error.message, code: error.code },
          { status: error.statusCode }
        )
      }
      return NextResponse.json(
        { success: false, error: "内部错误" },
        { status: 500 }
      )
    }
  }
}
```

***

#### 任务 7: 完善 netlify.toml

**工作量**: 2h
**负责人**: DevOps
**依赖**: 无
**验收标准**: Headers/Cache 配置

**具体步骤**:

1. 修改 `netlify.toml`
2. 添加 Cache Headers
3. 添加 Security Headers
4. 添加重定向规则
5. 测试配置

**代码位置**: `netlify.toml`

**配置内容**:

```toml
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
```

***

#### 任务 8: 修复 Prisma 连接池

**工作量**: 1h
**负责人**: 后端
**依赖**: Prisma
**验收标准**: 支持 pgbouncer

**具体步骤**:

1. 检查 `src/lib/prisma.ts`
2. 优化连接配置
3. 添加连接池参数说明
4. 测试连接稳定性

**代码位置**: `src/lib/prisma.ts`

***

## 三、验收检查清单

### 3.1 功能验收

| 检查项                       | 状态 | 验证方法                    |
| ------------------------- | -- | ----------------------- |
| usePermissions hook 可正常使用 | ⬜  | 前端测试                    |
| ErrorBoundary 捕获子组件错误     | ⬜  | 触发测试错误                  |
| Redis 生产环境强制验证            | ⬜  | 模拟生产环境                  |
| 数据库索引已添加                  | ⬜  | `prisma migrate status` |
| /api/health 返回健康状态        | ⬜  | 访问端点                    |
| API 统一使用 AppError         | ⬜  | 代码审查                    |

### 3.2 质量验收

| 检查项                  | 状态 | 达标标准    |
| -------------------- | -- | ------- |
| TypeScript strict 模式 | ⬜  | 0 error |
| ESLint               | ⬜  | 0 error |
| Build                | ⬜  | 成功      |
| API 响应 P95           | ⬜  | < 300ms |

***

## 四、版本日志

### 4.1 变更文件清单

```
新建文件:
- src/hooks/usePermissions.tsx
- src/components/error/ErrorBoundary.tsx
- src/lib/errors.ts
- src/lib/api-wrapper.ts
- src/app/api/health/route.ts

修改文件:
- src/lib/redis.ts
- prisma/schema.prisma
- netlify.toml
- src/app/admin/layout.tsx
```

### 4.2 部署检查

- [ ] `prisma migrate deploy` 执行成功
- [ ] 环境变量已在 Netlify 配置
- [ ] Redis 配置已验证
- [ ] 健康检查端点响应正常

***

## 五、风险与回滚

### 5.1 主要风险

| 风险         | 可能性 | 影响 | 应对          |
| ---------- | --- | -- | ----------- |
| migrate 失败 | 低   | 高  | 提前备份，准备回滚脚本 |
| Redis 配置错误 | 中   | 高  | 本地测试后再部署    |

### 5.2 回滚方案

```bash
# 数据库回滚
prisma migrate rollback

# 代码回滚
git revert <commit>

# Netlify 回滚
netlify deploy --prod --dir=.next
```

***

**文档版本**: v1.0
**完成标准**: 所有 P0 任务完成 + 验收通过
**负责人**: SoloSales Team
