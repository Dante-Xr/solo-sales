# M2: 商业起航 (v0.5.0) 工作执行计划

**项目版本**: v0.5.0
**里程碑**: M2 - 商业起航
**计划日期**: 2026-03-25
**目标日期**: 2026-04-15
**前置条件**: M1 (v0.4.3) 完成
**核心目标**: 构建转化率优化基础设施

---

## 一、里程碑概述

### 1.1 目标描述

v0.5.0 是商业增强版本，核心目标是构建转化率优化基础设施，通过评价系统、库存紧迫感、社交证明等组件提升用户信任感和购买紧迫感，从而提高转化率。

### 1.2 关键成果

| 类别 | 成果 |
|------|------|
| **评价系统** | 用户可提交/查看/管理商品评价 |
| **转化优化** | 库存紧迫感、社交证明、信任元素 |
| **SEO** | JSON-LD 结构化数据、动态 Meta |
| **合规** | Cookie Consent Banner |
| **性能** | TanStack Query、骨架屏 |

### 1.3 资源投入

| 角色 | 工作量 |
|------|--------|
| 前端 | 35h |
| 后端 | 10h |
| 设计 | 4h |
| **总计** | **49h** |

### 1.4 前置依赖

- [ ] M1: usePermissions hook 已恢复
- [ ] M1: ErrorBoundary 已添加
- [ ] M1: 数据库索引已添加

---

## 二、详细任务分解

### 2.1 P0 紧急任务

#### 任务 1: 商品评价系统 - 数据库

**工作量**: 2h
**负责人**: 后端
**依赖**: Prisma
**验收标准**: Review 模型创建

**具体步骤**:
1. 修改 `prisma/schema.prisma`
2. 添加 Review 模型
3. 添加 ReviewImage 模型
4. 添加 ReviewReply 模型
5. 执行 `prisma migrate dev`
6. 生成 TypeScript 类型

**代码位置**: `prisma/schema.prisma`

**模型设计**:
```prisma
model Review {
  id          String   @id @default(cuid())
  productId   String
  userId      String
  rating      Int      // 1-5 星
  title       String?
  content     String?
  images      ReviewImage[]
  isApproved  Boolean  @default(false)
  isFeatured  Boolean  @default(false)
  helpfulCount Int     @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  product     Product  @relation(fields: [productId], references: [id])
  user        User     @relation(fields: [userId], references: [id])
  replies     ReviewReply[]

  @@index([productId])
  @@index([userId])
  @@index([rating])
  @@index([createdAt])
}

model ReviewImage {
  id        String  @id @default(cuid())
  reviewId  String
  url       String
  position  Int     @default(0)

  review    Review  @relation(fields: [reviewId], references: [id], onDelete: Cascade)
}

model ReviewReply {
  id        String   @id @default(cuid())
  reviewId  String
  userId    String?
  adminId   String?
  content   String
  createdAt DateTime @default(now())

  review    Review  @relation(fields: [reviewId], references: [id], onDelete: Cascade)
}
```

---

#### 任务 2: 商品评价系统 - API

**工作量**: 4h
**负责人**: 后端
**依赖**: Review 模型
**验收标准**: CRUD + 审核API

**具体步骤**:
1. 创建 `src/app/api/reviews/route.ts` (创建/列表)
2. 创建 `src/app/api/reviews/[id]/route.ts` (详情/更新/删除)
3. 创建 `src/app/api/admin/reviews/route.ts` (审核管理)
4. 实现分页、筛选、排序
5. 实现权限控制
6. 添加缓存

**API 端点**:
```
GET    /api/reviews?productId=X&page=1&pageSize=10  # 公开列表
POST   /api/reviews                                  # 创建评价
GET    /api/reviews/[id]                            # 评价详情
PUT    /api/reviews/[id]                            # 更新评价
DELETE /api/reviews/[id]                            # 删除评价

GET    /api/admin/reviews?status=pending&page=1     # 审核列表
PUT    /api/admin/reviews/[id]/approve               # 审核通过
PUT    /api/admin/reviews/[id]/reject               # 审核拒绝
```

---

#### 任务 3: 商品评价系统 - 前端

**工作量**: 6h
**负责人**: 前端
**依赖**: 评价 API
**验收标准**: 评价列表/提交/回复

**具体步骤**:
1. 创建 `src/components/product/ReviewList.tsx`
2. 创建 `src/components/product/ReviewCard.tsx`
3. 创建 `src/components/product/ReviewForm.tsx`
4. 创建 `src/components/product/StarRating.tsx`
5. 创建 `src/components/admin/ReviewManagement.tsx`
6. 在商品详情页集成评价模块

**组件清单**:
```
src/components/product/
├── ReviewList.tsx        # 评价列表容器
├── ReviewCard.tsx        # 单个评价卡片
├── ReviewForm.tsx        # 评价提交表单
├── StarRating.tsx        # 星级展示/选择
└── ReviewSummary.tsx      # 评价汇总统计

src/components/admin/
└── ReviewManagement.tsx  # 后台评价审核
```

---

#### 任务 4: 库存紧迫感组件

**工作量**: 4h
**负责人**: 前端
**依赖**: 商品 API
**验收标准**: 低库存警告显示

**具体步骤**:
1. 创建 `src/components/product/UrgencyWidget.tsx`
2. 实现库存倒计时逻辑
3. 实现低库存警告样式
4. 在商品详情页集成
5. 添加动画效果

**组件设计**:
```typescript
interface UrgencyWidgetProps {
  stockCount: number
  lowStockThreshold?: number  // 默认 10
  salesCount?: number         // 日均销量
  restockDate?: Date
}

// 显示逻辑:
// stockCount <= lowStockThreshold: 显示 "仅剩 X 件"
// stockCount <= 3: 显示 "库存紧张，最后 X 件"
// restockDate: 显示 "预计 X 天后补货"
```

---

### 2.2 P1 重要任务

#### 任务 5: 社交证明 - 销量显示

**工作量**: 2h
**负责人**: 前端
**依赖**: 订单 API
**验收标准**: "已售 X 件"

**具体步骤**:
1. 创建 `src/components/product/SocialProof.tsx`
2. 获取商品销量数据
3. 在商品详情页集成
4. 添加动画效果

---

#### 任务 6: 社交证明 - 浏览记录

**工作量**: 2h
**负责人**: 前端
**依赖**: Redis
**验收标准**: "X 人正在浏览"

**具体步骤**:
1. 创建浏览事件追踪 API
2. 使用 Redis Sorted Set 存储浏览人数
3. 创建实时浏览组件
4. 在商品页集成

---

#### 任务 7: 信任元素组件

**工作量**: 2h
**负责人**: 前端
**依赖**: 无
**验收标准**: 安全徽章展示

**具体步骤**:
1. 创建 `src/components/product/TrustBadges.tsx`
2. 设计安全徽章样式
3. 在结账页集成
4. 响应式适配

**徽章内容**:
```
🔒 安全支付 - SSL 加密
🚚 全球配送 - 7-14 工作日
↩️ 30天退换 - 无理由退换
🔒 数据保护 - GDPR 合规
```

---

#### 任务 8: JSON-LD 结构化数据

**工作量**: 3h
**负责人**: 前端
**依赖**: 商品 API
**验收标准**: Google Rich Results

**具体步骤**:
1. 创建 `src/lib/seo/ProductSchema.ts`
2. 创建 `src/components/seo/ProductMeta.tsx`
3. 在商品详情页集成
4. 测试结构化数据

**实现要点**:
```typescript
// JSON-LD 结构化数据类型
{
  "@context": "https://schema.org",
  "@type": "Product",
  "name": "产品名称",
  "description": "产品描述",
  "image": ["图片URL"],
  "sku": "SKU",
  "brand": { "@type": "Brand", "name": "品牌" },
  "offers": {
    "@type": "Offer",
    "price": 99.00,
    "priceCurrency": "USD",
    "availability": "https://schema.org/InStock"
  },
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.5",
    "reviewCount": "89"
  }
}
```

---

#### 任务 9: Cookie Consent Banner

**工作量**: 2h
**负责人**: 前端
**依赖**: 无
**验收标准**: GDPR 合规

**具体步骤**:
1. 创建 `src/components/cookie/CookieConsent.tsx`
2. 实现本地状态存储
3. 创建 Cookie 政策页面
4. 在根布局集成
5. 国际化支持

---

#### 任务 10: TanStack Query 引入

**工作量**: 8h
**负责人**: 前端
**依赖**: 无
**验收标准**: 统一状态管理

**具体步骤**:
1. 安装 TanStack Query
2. 创建 `src/lib/query-provider.tsx`
3. 重构商品列表查询
4. 重构订单查询
5. 配置缓存策略
6. 移除部分 Context

**实施范围** (v0.5.0):
- 商品列表查询
- 商品详情查询
- 订单列表查询

---

#### 任务 11: 骨架屏组件

**工作量**: 4h
**负责人**: 前端
**依赖**: TanStack Query
**验收标准**: 加载体验优化

**具体步骤**:
1. 创建 `src/components/ui/Skeleton.tsx`
2. 创建 `src/components/product/ProductCardSkeleton.tsx`
3. 创建 `src/components/product/ProductDetailSkeleton.tsx`
4. 在商品列表页集成
5. 在商品详情页集成

---

### 2.3 P2 增强任务 (可选)

#### 任务 12: 动态 SEO Meta

**工作量**: 3h
**负责人**: 前端
**依赖**: 商品页
**验收标准**: og:title/description

**具体步骤**:
1. 扩展 ProductMeta 组件
2. 添加 Open Graph 标签
3. 添加 Twitter Card 标签
4. 添加 Canonical URL

---

#### 任务 13: 订单邮件通知

**工作量**: 6h
**负责人**: 后端
**依赖**: 订单 API
**验收标准**: 发送确认邮件

**具体步骤**:
1. 配置邮件服务 (Resend/SendGrid)
2. 创建邮件模板
3. 创建邮件发送服务
4. 在订单创建时触发
5. 测试邮件发送

---

## 三、验收检查清单

### 3.1 功能验收

| 检查项 | 状态 | 验证方法 |
|--------|------|----------|
| 用户可提交商品评价 | ⬜ | 前端测试 |
| 用户可查看商品评价列表 | ⬜ | 前端测试 |
| 管理员可审核评价 | ⬜ | 后台测试 |
| 库存 < 10 显示紧迫感警告 | ⬜ | 前端测试 |
| 商品页显示销量 | ⬜ | 前端测试 |
| 页面显示安全徽章 | ⬜ | 前端测试 |
| 商品页包含 JSON-LD | ⬜ | 结构化数据测试 |
| Cookie Consent 可弹出 | ⬜ | 前端测试 |

### 3.2 性能验收

| 检查项 | 状态 | 达标标准 |
|--------|------|----------|
| Lighthouse Score | ⬜ | > 85 |
| 骨架屏加载体验 | ⬜ | 无白屏 |

### 3.3 质量验收

| 检查项 | 状态 | 达标标准 |
|--------|------|----------|
| TypeScript | ⬜ | 0 error |
| ESLint | ⬜ | 0 error |
| Build | ⬜ | 成功 |

---

## 四、版本日志

### 4.1 变更文件清单

```
数据库变更:
- prisma/schema.prisma (Review/ReviewImage/ReviewReply)

新建文件:
- src/app/api/reviews/route.ts
- src/app/api/reviews/[id]/route.ts
- src/app/api/admin/reviews/route.ts
- src/app/api/health/route.ts
- src/components/product/ReviewList.tsx
- src/components/product/ReviewCard.tsx
- src/components/product/ReviewForm.tsx
- src/components/product/StarRating.tsx
- src/components/product/ReviewSummary.tsx
- src/components/product/UrgencyWidget.tsx
- src/components/product/SocialProof.tsx
- src/components/product/TrustBadges.tsx
- src/components/admin/ReviewManagement.tsx
- src/lib/seo/ProductSchema.ts
- src/components/seo/ProductMeta.tsx
- src/components/cookie/CookieConsent.tsx
- src/components/ui/Skeleton.tsx
- src/components/product/ProductCardSkeleton.tsx
- src/components/product/ProductDetailSkeleton.tsx
- src/lib/query-provider.tsx
```

---

## 五、风险与应对

### 5.1 主要风险

| 风险 | 可能性 | 影响 | 应对 |
|------|--------|------|------|
| 评价系统审核流程复杂 | 中 | 中 | 先上线后审核，简化流程 |
| TanStack Query 迁移影响现有代码 | 中 | 中 | 局部试点，逐步替换 |

### 5.2 回滚方案

```bash
# 数据库回滚
prisma migrate rollback

# 代码回滚
git revert <commit>
```

---

**文档版本**: v1.0
**完成标准**: 所有 P0 任务完成 + 验收通过
**前置里程碑**: M1 (v0.4.3)
**负责人**: SoloSales Team